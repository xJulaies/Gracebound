export function EditingBadge() {
  return (
    <span className="absolute right-1 top-1 z-20 rounded-full border border-accent bg-background/90 px-2 py-1 text-[0.625rem] font-semibold uppercase tracking-wide text-accent">
      Editing
    </span>
  );
}
